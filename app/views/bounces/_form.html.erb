<%= form_for(bounce) do |f| %>
  <% if bounce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bounce.errors.count, "error") %> prohibited this bounce from being saved:</h2>

      <ul>
      <% bounce.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
